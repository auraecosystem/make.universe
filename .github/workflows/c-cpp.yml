name: C/C++ CI

on:
  push:
    branches: [ "Makeuniversal.github.io" ]
  pull_request:
    branches: [ "Makeuniversal.github.io" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
